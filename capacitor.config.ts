import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'its.pair.bnb',
  appName: 'PlacesVisitorStore',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
